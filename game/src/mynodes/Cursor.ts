import AssetsManager from '../core/AssetsManager'
import Node from '../core/Node'
import Sprite from '../core/nodes/Sprite'

/*class Cursor extends Node{
	sprite: Sprite;

	public enter() : void {
		this.sprite = new Sprite(
			{x: 0, y: 0},
			{x: 30, y: 30},
			{sourceX: 0, sourceY: 0, sourceWidth: 30, sourceHeight: 30, offsetX: 0, offsetY: 0, width: 30, height: 30},
			AssetsManager.instance.getImage('cursor'),
		);

		this.addNode(this.sprite);
	}

	public update() : void {
		this.x = this.input.cursor.x;
		this.y = this.input.cursor.y;

		this.sprite.x = this.x;
		this.sprite.y = this.y;
	}
}

export default Cursor;*/
