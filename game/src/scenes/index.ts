import PreloadScene from './PreloadScene';
import MenuScene from './MenuScene';
import SandboxScene from './SandboxScene';

export default {
	preload: new PreloadScene(),
	menu: new MenuScene(),
	sandbox: new SandboxScene()
}